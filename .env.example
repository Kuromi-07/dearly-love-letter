# Copy this to .env.local and fill in your values
# All data is processed client-side; no backend secrets needed

# Public site URL (used for client-side configuration)
NEXT_PUBLIC_SITE_URL=http://localhost:3000
